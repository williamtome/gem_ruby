numeros = [4,7,2,6,4,1,8,2]

def merge_sort(arr, inicio, fim)
    if ((fim - inicio) > 1)
        meio = (fim + inicio) / 2
    end
end

inicio = numeros
fim = numeros.size - 1

merge_sort(numeros, inicio, fim)